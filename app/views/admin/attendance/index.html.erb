<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Attendance List</title>
</head>
<body>
  <div class="main-content">
    <%= render 'admin/shared/left_side_bar', attendances: 'active' %>

    <div class="right-side-content">
      <div class="container container-wrapper card">
        <div class="main-content-header">
          <h2>Attendance List</h2>
          <div class="main-content-header-inputs">
            <form class="form-inline my-2 my-lg-0 main-content-header-input">
              <%= render 'admin/shared/search_bar', placeholder: 'Search by employee' %>
              <div class="form-row main-content-header-input">
                <%= render 'admin/attendance/index/date_field', label_name: 'Start date', id: 'inputStartDate' %>
                <%= render 'admin/attendance/index/date_field', label_name: 'End date', id: 'inputEndDate' %>
              </div>
              <button class="main-content-header-input btn btn-dark">Search</button>
            </form>
          </div>
        </div>
        <%= render 'admin/attendance/index/attendance_list' %>
        <%= render 'shared/pagination' %>
      </div>
    </div>
  </div>
</body>
</html>
