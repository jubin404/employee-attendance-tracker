<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Employee</title>
</head>
<body>
  <div class="main-content">
    <%= render 'admin/shared/left_side_bar', employees: 'active' %>

    <div class="right-side-content">
      <div class="container container-wrapper card">
        <div class="main-content-header">
          <h2>Add Employee</h2>
        </div>
        <div class="employee-information">
          <form>
            <%= render 'admin/employee/shared/employee_form', editable: true, action: 'placeholder' %>
            <div class="main-content-footer col-md-8">
              <button type="submit" class="btn btn-danger">Cancel</button>
              <button type="submit" class="btn btn-success">Save</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
